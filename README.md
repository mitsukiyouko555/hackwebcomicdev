Note to self - when ready to merge with main directory, do the following:

1. git remote remove origin
2. git remote add origin https://github.com/mitsukiyouko555/hackwebcomic.git
3. Update the vite configs so the base is pointing to /hackwebcomic/
